# Local development (optional)
# If you run Functions separately, set a base URL:
# VITE_API_BASE_URL=http://localhost:7071
#
# If EasyAuth isn't enabled locally, you can inject fallback identity headers via Vite proxy:
# VITE_DEV_USER_ID=your-user-id
# VITE_DEV_USER_EMAIL=you@example.com
